/// <reference path="../../common.ts" />

interface BeaconchainValidatorEth1 {
  publickey: Hex;
  valid_signature: boolean;
  validatorindex: number;
}

/**
 * {{bc.paths["/api/v1/validator/eth1/{eth1address}"]["get"].summary}}
 *
 * @param {EthereumAddress} eth1address - {{bc.paths["/api/v1/validator/eth1/{eth1address}"]["get"].parameters[0].description}}
 * {{common.params.fields}}
 * {{common.params.offset}}
 * {{common.params.limit}}
 *
 * @customfunction
 */
function BC$VALIDATOR_ETH1(
  eth1address: EthereumAddress,
  fields: FieldsOrAll<BeaconchainValidatorEth1> = '*',
  offset?: number,
  limit?: number,
): SpreadsheetRow[] {
  return pickFields_<BeaconchainValidatorEth1>({
    rows: bcRequest_<BeaconchainValidatorEth1[]>({
      apiPath: `v1/validator/eth1/${eth1address}`,
      offset,
      limit,
    }),
    fields,
  });
}
